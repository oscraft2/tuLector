<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link de vuelta del OAuth (Supabase → https://tulector.vercel.app/auth/callback/app).
                 Android App Link verificado (autoVerify) en vez de un scheme custom:
                 un scheme custom (cl.tulector.app://) no es exclusivo — cualquier otra
                 app puede declarar el mismo scheme e interceptar el ?code= de OAuth.
                 Con autoVerify + assetlinks.json (public/.well-known/), el sistema
                 SOLO entrega el link a esta app si el dominio prueba ser dueño del
                 paquete (firma con nuestro keystore). Ver SECURITY_PROMPT_APK.md Tarea A.

                 La ruta es /auth/callback/APP, exclusiva de la app, y NO /auth/callback.
                 Mientras apuntaba a /auth/callback (que es tambien la ruta del flujo
                 web), Android le quitaba a Chrome su propio callback y se lo entregaba
                 a esta app: quien iniciaba sesion en Chrome en un telefono con el APK
                 instalado terminaba con "PKCE code verifier not found in storage",
                 porque el code_verifier se habia quedado en Chrome.
                 Ver docs/plan-apk-applink-fase2.md.

                 OJO al orden de despliegue: el APK carga la web REMOTA (server.url),
                 asi que la web debe estar desplegada ANTES de instalar este APK — es
                 la que decide, por el token de version del User-Agent, a que ruta
                 mandar el callback. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="tulector.vercel.app" android:pathPrefix="/auth/callback/app" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- READ/WRITE_EXTERNAL_STORAGE removidos: @capacitor/camera (unico
         consumidor de FileProvider en este proyecto) solo declara <queries>
         de intents en su manifest, sin uses-permission de almacenamiento; y
         captureNativePhoto()/shareNativeImage() en capacitor.ts trabajan con
         dataUrl/base64 en memoria, nunca tocan el filesystem externo. Menos
         permisos = menos friccion en revision de Play y mejor Data Safety.
         Ver SECURITY_PROMPT_APK.md Tarea H. -->
    <!-- BiometricPrompt (login con huella): el plugin
         @aparajita/capacitor-biometric-auth NO declara este permiso en su propio
         manifest, asi que lo agregamos aca. Sin USE_BIOMETRIC, authenticate() falla
         con "BiometricPrompt is not available". USE_FINGERPRINT es legacy (API <28)
         pero se mantiene por compatibilidad con Android 8. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Publicidad DESACTIVADA: la app no usa ads. Google Play Services / Auth
         agregan este permiso por dependencias transitivas; lo removemos para no
         tener que declarar "Advertising ID" en Data Safety (y evitar rechazos). -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
</manifest>
