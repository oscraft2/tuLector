name: tulector
description: TuLector - Escáner OMR de exámenes con motor C++ nativo.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  ffi: ^2.1.0
  provider: ^6.1.0
  path_provider: ^2.1.0
  vibration: ^2.0.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
